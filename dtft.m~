function X = dtft(a,b,n)
    output = zeros(1,2001);
    for m = 1:length(b)
        X(m) = 0; % assigning to zero
        for k = 1:length(n)
            X(m) = X(m)+a(k).*exp(-sqrt(-1).*b(m).*n(k));
        end
    end
    X = ouput;
en

